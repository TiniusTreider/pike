# pike

UCI compatible chess engine.

## usage

```pike``` runs UCI mode.

## build

run ```make``` in project root on Unix systems to compile, and ```make clean``` to remove build files and compiled binary.
pike is not yet Windows-compatible.

