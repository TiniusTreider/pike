#!/usr/bin/env bash

> t/log.log
fastchess \
    -engine name=pike_new proto=uci cmd=$1 \
    -engine name=pike_old proto=uci cmd=$2 \
    -openings file=books/8mvs_+90_+99.epd format=epd order=random \
    -games 1000 \
    -concurrency 8 \
    -each tc=0.13333+0.08 \
    -log file=t/log.log \
    -log engine=true

